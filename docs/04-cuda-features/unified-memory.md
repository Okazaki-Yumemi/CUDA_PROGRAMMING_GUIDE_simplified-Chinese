---
title: 4.1 Unified Memory
description: 统一内存的地址、迁移与性能
---

# 4.1 Unified Memory

统一内存把 CPU 与 GPU 的访问组织到统一虚拟地址空间中，运行时或硬件在需要时迁移/安排数据。它适合降低 host/device 指针管理复杂度，但不是没有迁移成本；预取、访问提示、并发访问和互连都会影响性能。

先用统一内存验证数据结构和算法，再用 profiler 判断是否需要显式复制、预取或更精细的驻留策略。
